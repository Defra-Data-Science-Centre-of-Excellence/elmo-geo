name: Verify

on:
  pull_request:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: databrickslabs/mosaic/scala_build
      - uses: databrickslabs/mosaic/python_build
      - run: make install
      - run: make verify
